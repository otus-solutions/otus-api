FROM mongo:4.0

ARG INITDB_FILE=initdb.sh

ENV ROOT_USER="otus"
ENV ROOT_PASS="otus"
ENV MONGO_INITDB_DATABASE=admin
ENV MONGO_INITDB_ROOT_USERNAME=${ROOT_USER}
ENV MONGO_INITDB_ROOT_PASSWORD=${ROOT_PASS}

ADD ${INITDB_FILE} /docker-entrypoint-initdb.d/

EXPOSE 27017
